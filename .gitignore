# AgentLocalConfig.swift ships empty and IS tracked — fill it in locally,
# and do not commit your real keys.

# Local secrets
.env
.env.*
*.p8
*.p12
*.mobileprovision

# Xcode
xcuserdata/
*.xcuserdatad/
*.xcuserstate
DerivedData/
build/
*.hmap
*.ipa
*.dSYM.zip
*.moved-aside

# SwiftPM local state (the pinned Package.resolved in the workspace IS tracked)
.build/
.swiftpm/
RainAgentDemo.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/configuration/

# macOS
.DS_Store
